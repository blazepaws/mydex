{
  "name": "Rust",
  "dockerComposeFile": "docker-compose.yml",
  "service": "rust",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "python3 -m venv .venv; source .venv/bin/activate; python -m pip install -r requirements.txt"
}